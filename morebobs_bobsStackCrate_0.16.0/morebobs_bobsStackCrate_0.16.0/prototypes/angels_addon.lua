--stacking
if mods["DeadlockStacking"] then
	--Angels & UP ores
	if data.raw["item"]["angels-ore1"] then 
		deadlock_stacking.create("angels-ore1", "__morebobs_bobsStackCrate__/graphics/icons/stacking/ores/angel/angels-ore1.png", "deadlock-stacking-1", 32)
	end
	if data.raw["item"]["angels-ore2"] then 
		deadlock_stacking.create("angels-ore2", "__morebobs_bobsStackCrate__/graphics/icons/stacking/ores/angel/angels-ore2.png", "deadlock-stacking-1", 32)
	end
	if data.raw["item"]["angels-ore3"] then 
		deadlock_stacking.create("angels-ore3", "__morebobs_bobsStackCrate__/graphics/icons/stacking/ores/angel/angels-ore3.png", "deadlock-stacking-1", 32)
	end
	if data.raw["item"]["angels-ore4"] then 
		deadlock_stacking.create("angels-ore4", "__morebobs_bobsStackCrate__/graphics/icons/stacking/ores/angel/angels-ore4.png", "deadlock-stacking-1", 32)
	end
	if data.raw["item"]["angels-ore5"] then 
		deadlock_stacking.create("angels-ore5", "__morebobs_bobsStackCrate__/graphics/icons/stacking/ores/angel/angels-ore5.png", "deadlock-stacking-1", 32)
	end
	if data.raw["item"]["angels-ore6"] then 
		deadlock_stacking.create("angels-ore6", "__morebobs_bobsStackCrate__/graphics/icons/stacking/ores/angel/angels-ore6.png", "deadlock-stacking-1", 32)
	end

	if data.raw["item"]["up-fluorite"] then 
		deadlock_stacking.create("up-fluorite", "__morebobs_bobsStackCrate__/graphics/icons/stacking/ores/uranium/up-fluorite.png", "deadlock-stacking-1", 32)
	end
	if data.raw["item"]["up-uraninite"] then 
		deadlock_stacking.create("up-uraninite", "__morebobs_bobsStackCrate__/graphics/icons/stacking/ores/uranium/up-uraninite.png", "deadlock-stacking-1", 32)
	end
	
	if data.raw["item"]["chrome-ore"] then 
		deadlock_stacking.create("chrome-ore", "__morebobs_bobsStackCrate__/graphics/icons/stacking/ores/angel/ore-chrome.png", "deadlock-stacking-1")
	end
	if data.raw["item"]["manganese-ore"] then 
		deadlock_stacking.create("manganese-ore", "__morebobs_bobsStackCrate__/graphics/icons/stacking/ores/angel/ore-manganese.png", "deadlock-stacking-1")
	end
	if data.raw["item"]["platinum-ore"] then 
		deadlock_stacking.create("platinum-ore", "__morebobs_bobsStackCrate__/graphics/icons/stacking/ores/angel/ore-platinum.png", "deadlock-stacking-1")
	end

	--Angel's crushed
	if data.raw["item"]["angels-ore1-crushed"] then 
		deadlock_stacking.create("angels-ore1-crushed", "__morebobs_bobsStackCrate__/graphics/icons/stacking/ores/crushed/angels-ore1-crushed.png", "deadlock-stacking-1", 32)
	end
	if data.raw["item"]["angels-ore2-crushed"] then 
		deadlock_stacking.create("angels-ore2-crushed", "__morebobs_bobsStackCrate__/graphics/icons/stacking/ores/crushed/angels-ore2-crushed.png", "deadlock-stacking-1", 32)
	end
	if data.raw["item"]["angels-ore3-crushed"] then 
		deadlock_stacking.create("angels-ore3-crushed", "__morebobs_bobsStackCrate__/graphics/icons/stacking/ores/crushed/angels-ore3-crushed.png", "deadlock-stacking-1", 32)
	end
	if data.raw["item"]["angels-ore4-crushed"] then 
		deadlock_stacking.create("angels-ore4-crushed", "__morebobs_bobsStackCrate__/graphics/icons/stacking/ores/crushed/angels-ore4-crushed.png", "deadlock-stacking-1", 32)
	end
	if data.raw["item"]["angels-ore5-crushed"] then 
		deadlock_stacking.create("angels-ore5-crushed", "__morebobs_bobsStackCrate__/graphics/icons/stacking/ores/crushed/angels-ore5-crushed.png", "deadlock-stacking-1", 32)
	end
	if data.raw["item"]["angels-ore6-crushed"] then 
		deadlock_stacking.create("angels-ore6-crushed", "__morebobs_bobsStackCrate__/graphics/icons/stacking/ores/crushed/angels-ore6-crushed.png", "deadlock-stacking-1", 32)
	end
	if data.raw["item"]["angels-ore8-crushed"] then 
		deadlock_stacking.create("angels-ore8-crushed", "__morebobs_bobsStackCrate__/graphics/icons/stacking/ores/crushed/angels-ore8-crushed.png", "deadlock-stacking-1", 32)
	end
	if data.raw["item"]["angels-ore9-crushed"] then 
		deadlock_stacking.create("angels-ore9-crushed", "__morebobs_bobsStackCrate__/graphics/icons/stacking/ores/crushed/angels-ore9-crushed.png", "deadlock-stacking-1", 32)
	end
	if data.raw["item"]["copper-ore-crushed"] then 
		deadlock_stacking.create("copper-ore-crushed", "__morebobs_bobsStackCrate__/graphics/icons/stacking/ores/crushed/copper-ore-crushed.png", "deadlock-stacking-1", 32)
	end
	if data.raw["item"]["iron-ore-crushed"] then 
		deadlock_stacking.create("iron-ore-crushed", "__morebobs_bobsStackCrate__/graphics/icons/stacking/ores/crushed/iron-ore-crushed.png", "deadlock-stacking-1", 32)
	end
	if data.raw["item"]["nodule-crushed"] then 
		deadlock_stacking.create("nodule-crushed", "__morebobs_bobsStackCrate__/graphics/icons/stacking/ores/crushed/nodule-crushed.png", "deadlock-stacking-1", 32)
	end
	if data.raw["item"]["stone-crushed"] then 
		deadlock_stacking.create("stone-crushed", nil, "deadlock-stacking-1")
	end

	--Angels Raw and Intermediate product
	if data.raw["item"]["solid-salt"] then 
		deadlock_stacking.create("solid-salt", nil, "deadlock-stacking-4")
	end
	if data.raw["item"]["solid-clay"] then 
		deadlock_stacking.create("solid-clay", nil, "deadlock-stacking-4")
	end
	if data.raw["item"]["solid-limestone"] then 
		deadlock_stacking.create("solid-limestone", nil, "deadlock-stacking-4")
	end
	if data.raw["item"]["solid-sand"] then 
		deadlock_stacking.create("solid-sand", nil, "deadlock-stacking-4")
	end
	if data.raw["item"]["crystal-dust"] then 
		deadlock_stacking.create("crystal-dust", nil, "deadlock-stacking-4")
	end

	--Angels Refining 1
	if data.raw["item"]["angels-ore1-chunk"] then 
		deadlock_stacking.create("angels-ore1-chunk", nil, "deadlock-stacking-2")
	end
	if data.raw["item"]["angels-ore2-chunk"] then 
		deadlock_stacking.create("angels-ore2-chunk", nil, "deadlock-stacking-2")
	end
	if data.raw["item"]["angels-ore3-chunk"] then 
		deadlock_stacking.create("angels-ore3-chunk", nil, "deadlock-stacking-2")
	end
	if data.raw["item"]["angels-ore4-chunk"] then 
		deadlock_stacking.create("angels-ore4-chunk", nil, "deadlock-stacking-2")
	end
	if data.raw["item"]["angels-ore5-chunk"] then 
		deadlock_stacking.create("angels-ore5-chunk", nil, "deadlock-stacking-2")
	end
	if data.raw["item"]["angels-ore6-chunk"] then 
		deadlock_stacking.create("angels-ore6-chunk", nil, "deadlock-stacking-2")
	end
	if data.raw["item"]["angels-ore8-powder"] then 
		deadlock_stacking.create("angels-ore8-powder", nil, "deadlock-stacking-2")
	end
	if data.raw["item"]["angels-ore9-powder"] then 
		deadlock_stacking.create("angels-ore9-powder", nil, "deadlock-stacking-2")
	end

	--Angels Refining 2
	if data.raw["item"]["angels-ore1-crystal"] then 
		deadlock_stacking.create("angels-ore1-crystal", nil, "deadlock-stacking-3")
	end
	if data.raw["item"]["angels-ore2-crystal"] then 
		deadlock_stacking.create("angels-ore2-crystal", nil, "deadlock-stacking-3")
	end
	if data.raw["item"]["angels-ore3-crystal"] then 
		deadlock_stacking.create("angels-ore3-crystal", nil, "deadlock-stacking-3")
	end
	if data.raw["item"]["angels-ore4-crystal"] then 
		deadlock_stacking.create("angels-ore4-crystal", nil, "deadlock-stacking-3")
	end
	if data.raw["item"]["angels-ore5-crystal"] then 
		deadlock_stacking.create("angels-ore5-crystal", nil, "deadlock-stacking-3")
	end
	if data.raw["item"]["angels-ore6-crystal"] then 
		deadlock_stacking.create("angels-ore6-crystal", nil, "deadlock-stacking-3")
	end
	if data.raw["item"]["angels-ore8-dust"] then 
		deadlock_stacking.create("angels-ore8-dust", nil, "deadlock-stacking-3")
	end
	if data.raw["item"]["angels-ore9-dust"] then 
		deadlock_stacking.create("angels-ore9-dust", nil, "deadlock-stacking-3")
	end

	--Angels Refining 3
	if data.raw["item"]["angels-ore1-pure"] then 
		deadlock_stacking.create("angels-ore1-pure", nil, "deadlock-stacking-4")
	end
	if data.raw["item"]["angels-ore2-pure"] then 
		deadlock_stacking.create("angels-ore2-pure", nil, "deadlock-stacking-4")
	end
	if data.raw["item"]["angels-ore3-pure"] then 
		deadlock_stacking.create("angels-ore3-pure", nil, "deadlock-stacking-4")
	end
	if data.raw["item"]["angels-ore4-pure"] then 
		deadlock_stacking.create("angels-ore4-pure", nil, "deadlock-stacking-4")
	end
	if data.raw["item"]["angels-ore5-pure"] then 
		deadlock_stacking.create("angels-ore5-pure", nil, "deadlock-stacking-4")
	end
	if data.raw["item"]["angels-ore6-pure"] then 
		deadlock_stacking.create("angels-ore6-pure", nil, "deadlock-stacking-4")
	end
	if data.raw["item"]["angels-ore8-crystal"] then 
		deadlock_stacking.create("angels-ore8-crystal", nil, "deadlock-stacking-4")
	end
	if data.raw["item"]["angels-ore9-crystal"] then 
		deadlock_stacking.create("angels-ore9-crystal", nil, "deadlock-stacking-4")
	end

	--Angels Plate and Alloy
	if data.raw["item"]["angels-plate-chrome"] then 
		deadlock_stacking.create("angels-plate-chrome", nil, "deadlock-stacking-5")
	end
	if data.raw["item"]["angels-plate-manganese"] then 
		deadlock_stacking.create("angels-plate-manganese", nil, "deadlock-stacking-5")
	end
	if data.raw["item"]["angels-plate-platinum"] then 
		deadlock_stacking.create("angels-plate-platinum", nil, "deadlock-stacking-5")
	end
end

--crating
if mods["DeadlockCrating"] then
	--Angels & UP ores
	if data.raw["item"]["angels-ore1"] then 
		deadlock_crating.create("angels-ore1", "deadlock-crating-1")
	end
	if data.raw["item"]["angels-ore2"] then 
		deadlock_crating.create("angels-ore2", "deadlock-crating-1")
	end
	if data.raw["item"]["angels-ore3"] then 
		deadlock_crating.create("angels-ore3", "deadlock-crating-1")
	end
	if data.raw["item"]["angels-ore4"] then 
		deadlock_crating.create("angels-ore4", "deadlock-crating-1")
	end
	if data.raw["item"]["angels-ore5"] then 
		deadlock_crating.create("angels-ore5", "deadlock-crating-1")
	end
	if data.raw["item"]["angels-ore6"] then 
		deadlock_crating.create("angels-ore6", "deadlock-crating-1")
	end

	if data.raw["item"]["up-fluorite"] then 
		deadlock_crating.create("up-fluorite", "deadlock-crating-1")
	end
	if data.raw["item"]["up-uraninite"] then 
		deadlock_crating.create("up-uraninite", "deadlock-crating-1")
	end

	if data.raw["item"]["chrome-ore"] then 
		deadlock_crating.create("chrome-ore", "deadlock-crating-1")
	end
	if data.raw["item"]["manganese-ore"] then 
		deadlock_crating.create("manganese-ore", "deadlock-crating-1")
	end
	if data.raw["item"]["platinum-ore"] then 
		deadlock_crating.create("platinum-ore", "deadlock-crating-1")
	end

	if data.raw["item"]["angels-ore1-crushed"] then 
		deadlock_crating.create("angels-ore1-crushed", "deadlock-crating-1")
	end
	if data.raw["item"]["angels-ore2-crushed"] then 
		deadlock_crating.create("angels-ore2-crushed", "deadlock-crating-1")
	end
	if data.raw["item"]["angels-ore3-crushed"] then 
		deadlock_crating.create("angels-ore3-crushed", "deadlock-crating-1")
	end
	if data.raw["item"]["angels-ore4-crushed"] then 
		deadlock_crating.create("angels-ore4-crushed", "deadlock-crating-1")
	end
	if data.raw["item"]["angels-ore5-crushed"] then 
		deadlock_crating.create("angels-ore5-crushed", "deadlock-crating-1")
	end
	if data.raw["item"]["angels-ore6-crushed"] then 
		deadlock_crating.create("angels-ore6-crushed", "deadlock-crating-1")
	end
	if data.raw["item"]["angels-ore8-crushed"] then 
		deadlock_crating.create("angels-ore8-crushed", "deadlock-crating-1")
	end
	if data.raw["item"]["angels-ore9-crushed"] then 
		deadlock_crating.create("angels-ore9-crushed", "deadlock-crating-1")
	end
	if data.raw["item"]["copper-ore-crushed"] then 
		deadlock_crating.create("copper-ore-crushed", "deadlock-crating-1")
	end
	if data.raw["item"]["iron-ore-crushed"] then 
		deadlock_crating.create("iron-ore-crushed", "deadlock-crating-1")
	end
	if data.raw["item"]["nodule-crushed"] then 
		deadlock_crating.create("nodule-crushed", "deadlock-crating-1")
	end
	if data.raw["item"]["stone-crushed"] then 
		deadlock_crating.create("stone-crushed", "deadlock-crating-1")
	end

	--Angels Raw and Intermediate product
	if data.raw["item"]["solid-salt"] then 
		deadlock_crating.create("solid-salt", "deadlock-crating-4")
	end
	if data.raw["item"]["solid-clay"] then 
		deadlock_crating.create("solid-clay", "deadlock-crating-4")
	end
	if data.raw["item"]["solid-limestone"] then 
		deadlock_crating.create("solid-limestone", "deadlock-crating-4")
	end
	if data.raw["item"]["solid-sand"] then 
		deadlock_crating.create("solid-sand", "deadlock-crating-4")
	end
	if data.raw["item"]["crystal-dust"] then 
		deadlock_crating.create("crystal-dust", "deadlock-crating-4")
	end	

	--Angels Refining 1
	if data.raw["item"]["angels-ore1-chunk"] then 
		deadlock_crating.create("angels-ore1-chunk", "deadlock-crating-2")
	end
	if data.raw["item"]["angels-ore2-chunk"] then 
		deadlock_crating.create("angels-ore2-chunk", "deadlock-crating-2")
	end
	if data.raw["item"]["angels-ore3-chunk"] then 
		deadlock_crating.create("angels-ore3-chunk", "deadlock-crating-2")
	end
	if data.raw["item"]["angels-ore4-chunk"] then 
		deadlock_crating.create("angels-ore4-chunk", "deadlock-crating-2")
	end
	if data.raw["item"]["angels-ore5-chunk"] then 
		deadlock_crating.create("angels-ore5-chunk", "deadlock-crating-2")
	end
	if data.raw["item"]["angels-ore6-chunk"] then 
		deadlock_crating.create("angels-ore6-chunk", "deadlock-crating-2")
	end
	if data.raw["item"]["angels-ore8-powder"] then 
		deadlock_crating.create("angels-ore8-powder", "deadlock-crating-2")
	end
	if data.raw["item"]["angels-ore9-powder"] then 
		deadlock_crating.create("angels-ore9-powder", "deadlock-crating-2")
	end

	--Angels Refining 2
	if data.raw["item"]["angels-ore1-crystal"] then 
		deadlock_crating.create("angels-ore1-crystal", "deadlock-crating-3")
	end
	if data.raw["item"]["angels-ore2-crystal"] then 
		deadlock_crating.create("angels-ore2-crystal", "deadlock-crating-3")
	end
	if data.raw["item"]["angels-ore3-crystal"] then 
		deadlock_crating.create("angels-ore3-crystal", "deadlock-crating-3")
	end
	if data.raw["item"]["angels-ore4-crystal"] then 
		deadlock_crating.create("angels-ore4-crystal", "deadlock-crating-3")
	end
	if data.raw["item"]["angels-ore5-crystal"] then 
		deadlock_crating.create("angels-ore5-crystal", "deadlock-crating-3")
	end
	if data.raw["item"]["angels-ore6-crystal"] then 
		deadlock_crating.create("angels-ore6-crystal", "deadlock-crating-3")
	end
	if data.raw["item"]["angels-ore8-dust"] then 
		deadlock_crating.create("angels-ore8-dust", "deadlock-crating-3")
	end
	if data.raw["item"]["angels-ore9-dust"] then 
		deadlock_crating.create("angels-ore9-dust", "deadlock-crating-3")
	end

	--Angels Refining 3
	if data.raw["item"]["angels-ore1-pure"] then 
		deadlock_crating.create("angels-ore1-pure", "deadlock-crating-4")
	end
	if data.raw["item"]["angels-ore2-pure"] then 
		deadlock_crating.create("angels-ore2-pure", "deadlock-crating-4")
	end
	if data.raw["item"]["angels-ore3-pure"] then 
		deadlock_crating.create("angels-ore3-pure", "deadlock-crating-4")
	end
	if data.raw["item"]["angels-ore4-pure"] then 
		deadlock_crating.create("angels-ore4-pure", "deadlock-crating-4")
	end
	if data.raw["item"]["angels-ore5-pure"] then 
		deadlock_crating.create("angels-ore5-pure", "deadlock-crating-4")
	end
	if data.raw["item"]["angels-ore6-pure"] then 
		deadlock_crating.create("angels-ore6-pure", "deadlock-crating-4")
	end
	if data.raw["item"]["angels-ore8-crystal"] then 
		deadlock_crating.create("angels-ore8-crystal", "deadlock-crating-4")
	end
	if data.raw["item"]["angels-ore9-crystal"] then 
		deadlock_crating.create("angels-ore9-crystal", "deadlock-crating-4")
	end

	--Angels Plate and Alloy
	if data.raw["item"]["angels-plate-chrome"] then 
		deadlock_crating.create("angels-plate-chrome", "deadlock-crating-5")
	end
	if data.raw["item"]["angels-plate-manganese"] then 
		deadlock_crating.create("angels-plate-manganese", "deadlock-crating-5")
	end
	if data.raw["item"]["angels-plate-platinum"] then 
		deadlock_crating.create("angels-plate-platinum", "deadlock-crating-5")
	end
end